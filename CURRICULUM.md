# Rust学習カリキュラム

Go経験者が、Rust固有の所有権、型、エラー処理、並行処理を段階的に学ぶためのカリキュラムです。

## 到達目標

8週間を目安に、次のことができる状態を目指します。

- RustらしいCLIアプリケーションを作成できる
- ファイル、JSON、HTTPを扱える
- 所有権と借用を意識してAPIを設計できる
- ジェネリクスとトレイトを使い分けられる
- `async`を使った小規模なWeb APIを作成できる
- フォーマット、lint、テストを開発手順に組み込める

学習時間の目安は、週5日、1日30〜60分です。

## 第1週：開発環境と基本文法

### 学習項目

- [ ] Rustのツールチェーンを準備する
  - `rustup show`で使用中のtoolchainを確認する
  - `rustc --version`と`cargo --version`でインストールを確認する
  - `rustup update`でtoolchainを更新できることを確認する
  - `rustup component add rustfmt clippy`で開発用componentを追加する
  - `rustc`はコンパイラ、Cargoはビルド・依存管理・テストを担うと説明できるようにする
- [ ] Cargo packageの構造と基本操作を覚える
  - `cargo new`でbinary packageとlibrary packageをそれぞれ作る
  - `Cargo.toml`、`Cargo.lock`、`src/main.rs`、`src/lib.rs`の役割を確認する
  - `cargo check`、`cargo build`、`cargo run`の用途と速度の違いを確認する
  - `cargo add`で依存crateを追加し、`Cargo.toml`の変化を読む
- [ ] 変数、可変性、定数、シャドーイングを試す
  - `let`で宣言した変数を変更してコンパイルエラーを確認する
  - `let mut`で値を変更する
  - 同じ変数名を再度`let`し、型も変更できるシャドーイングを試す
  - `const`の型注釈と命名規則を確認し、`let`との違いを説明する
- [ ] 基本型と複合型を操作する
  - 整数、浮動小数点数、`bool`、`char`に明示的な型注釈を書く
  - 整数の範囲と、debug/releaseビルドにおけるオーバーフローの扱いを確認する
  - タプルの分配束縛と`.0`形式のアクセスを試す
  - 固定長配列を作り、配列全体からスライスを借用する
- [ ] 制御構文を使い分ける
  - `if`の各分岐が同じ型を返す必要があることを確認する
  - `loop`から`break 値`で値を返す
  - 条件反復には`while`、コレクションの反復には`for`を使う
  - `0..5`と`0..=5`の範囲の違いを確認する
- [ ] 関数、文、式の違いを理解する
  - 引数と戻り値の型を明示した関数を書く
  - 末尾式で値を返し、セミコロンを付けた場合の型エラーを確認する
  - ブロックを式として変数へ代入する
- [ ] 基本的な品質チェックを実行する
  - `cargo fmt --check`でフォーマットを検査する
  - `cargo clippy -- -D warnings`で警告をエラーとして検査する
  - `#[test]`、`assert_eq!`、`assert!`を使った単体テストを書く
  - `cargo test`でテスト名、成功数、失敗内容を読む

### 演習

- [ ] FizzBuzz
- [ ] 素数判定
- [ ] 文字列中の単語数カウント
- [ ] 各処理の単体テスト

### Goとの違い

Rustの変数はデフォルトで変更できません。変更する場合は`mut`を明示します。また、`if`などの式から値を返せます。

```rust
let x = 1;
let mut y = 1;
y += 1;

let result = if y > x { "large" } else { "small" };
```

## 第2週：所有権と借用

この週はRust学習で最も重要な部分です。

### 学習項目

- [ ] 所有権とスコープによる解放を理解する
  - `String`を作り、スコープを抜けると`drop`される流れを説明する
  - 値を関数へ渡す、関数から返す、別変数へ代入する操作で所有者がどう変わるか追跡する
  - ヒープ上のデータがGCなしで安全に解放される理由を説明する
- [ ] ムーブ、`Copy`、`clone`を区別する
  - `String`を別変数へ代入した後、元の変数が使えないことを確認する
  - 整数や`bool`では代入後も元の変数を使えることを確認する
  - `Copy`は暗黙のビットコピー、`clone`は明示的な複製だと説明する
  - コンパイルを通すためだけの安易な`clone()`を避ける
- [ ] 共有参照`&T`で値を借用する
  - 所有権を受け取らずに文字列の長さを返す関数を書く
  - 同じ値に複数の共有参照を作れることを確認する
  - 参照から所有している値を勝手にムーブできないことを確認する
- [ ] 可変参照`&mut T`で値を変更する
  - `&mut String`を受け取り、文字列を追記する関数を書く
  - 一つの値に同時に作れる可変参照は一つだけであることを確認する
  - 共有参照の利用期間と可変参照の利用期間が重ならないようスコープを調整する
- [ ] 借用規則と参照の有効期間を読み取る
  - 「複数の共有参照」または「一つの可変参照」という規則をコードで確認する
  - non-lexical lifetimeにより、参照が最後に使われた時点で借用が終了する例を試す
  - dangling referenceを作ろうとしてコンパイラが拒否することを確認する
  - エラーの変数名と行番号から、競合する借用を特定する
- [ ] `String`と`&str`を使い分ける
  - 文字列リテラル、`String::from`、`to_string`で値を作る
  - UTF-8のため文字列を整数インデックスで参照できない理由を理解する
  - `&s[start..end]`の境界が文字境界でなければ失敗することを確認する
  - 所有する必要がない関数引数では、まず`&str`を検討する
- [ ] ライフタイム注釈の目的を理解する
  - 参照を一つ受け取って返す関数では注釈を省略できることを確認する
  - 二つの参照から一方を返す関数に`'a`を付ける
  - ライフタイムは参照の寿命を延ばす機能ではなく、参照間の関係を示すものだと説明する
  - 参照を持つ構造体にライフタイムパラメータを付ける

### 演習

- [ ] `String`を受け取る関数と`&str`を受け取る関数を比較する
- [ ] ベクタから最大値への参照を返す
- [ ] 文字列から最初の単語を`&str`で返す
- [ ] 意図的に借用エラーを発生させ、原因を説明して修正する
- [ ] 不要な`clone()`を借用に置き換える

### Goとの違い

GoのGCに相当する仕組みを探すのではなく、値の所有者と借用期間を確認します。

## 第3週：構造体、列挙型、パターンマッチ

### 学習項目

- [ ] `struct`でドメインデータを定義する
  - 名前付きfieldを持つ構造体を定義し、インスタンスを生成する
  - field init shorthandとstruct update構文を使う
  - tuple structとunit-like structの用途を確認する
  - 所有するfieldと参照を持つfieldの違いを確認する
- [ ] `impl`に振る舞いをまとめる
  - `Self`を返すコンストラクタ相当の関連関数`new`を書く
  - `&self`、`&mut self`、`self`を受け取るメソッドを一つずつ作る
  - receiverの違いが、参照・変更・消費のどれを表すか説明する
  - データを公開fieldへ直接書き込まず、メソッド経由で不変条件を守る
- [ ] `enum`で取り得る状態を表現する
  - 単純な列挙値と、各variantが異なるデータを持つ列挙型を作る
  - 不正な状態を作れないよう、複数の`bool`を`enum`へ置き換える
  - `impl`で列挙型にもメソッドを定義する
- [ ] `Option<T>`で値がない可能性を表現する
  - `Some`と`None`を生成し、`match`で両方を処理する
  - `map`、`and_then`、`unwrap_or`を一つずつ試す
  - `unwrap`でpanicする条件を理解し、通常の分岐では安全な代替手段を選ぶ
  - Goの`nil`や`(value, ok)`との違いを説明する
- [ ] パターンマッチを使い分ける
  - `match`ですべてのvariantを網羅し、網羅性検査を確認する
  - 一つのパターンだけ処理する場合に`if let`を使う
  - 条件を満たさない場合に早期returnする処理を`let else`で書く
  - `_`、値の束縛、構造体の分配、match guardを試す
- [ ] crate、module、path、可視性を理解する
  - `lib.rs`から子moduleを宣言し、複数ファイルへ分割する
  - 絶対pathの`crate::`と相対pathの`self::`、`super::`を使う
  - `use`で名前を現在のscopeへ導入する
  - 非公開を基本とし、外部APIに必要な項目だけ`pub`にする
  - binary crateから同じpackageのlibrary crateを利用する

### 演習

- [ ] ユーザー管理モデルを作る
- [ ] 四則演算を表す`enum`と評価関数を作る
- [ ] 注文の状態遷移を`enum`で表す
- [ ] `Option`を使ったユーザー検索を実装する

### Goとの対応

| Go | Rust |
| --- | --- |
| `struct` | `struct` |
| メソッド | `impl` |
| `(value, ok)` | `Option<T>` |
| type switch | `enum`と`match` |
| package | crateとmodule |

## 第4週：コレクションとエラー処理

### 学習項目

- [ ] `Vec<T>`と`HashMap<K, V>`を安全に操作する
  - `Vec`の生成、`push`、反復、`get`による安全な要素取得を試す
  - `vec[index]`と`vec.get(index)`の失敗時の違いを確認する
  - `HashMap`へ値を追加し、`get`と`entry` APIで取得・更新する
  - コレクションが値を所有する場合と参照を保存する場合の違いを確認する
- [ ] イテレータとクロージャでデータを変換する
  - `iter`、`iter_mut`、`into_iter`が返す要素の型と所有権の違いを確認する
  - `map`、`filter`、`enumerate`、`collect`で変換処理を書く
  - `fold`または`sum`で集約処理を書く
  - イテレータが遅延評価され、consumerを呼ぶまで処理されないことを確認する
  - クロージャが環境を借用、可変借用、ムーブする例を作る
- [ ] `Option<T>`と`Result<T, E>`を使い分ける
  - 値が存在しないだけなら`Option`、失敗理由を返すなら`Result`を選ぶ
  - `match`とcombinatorの両方で成功・失敗を処理する
  - `ok_or`と`ok_or_else`で`Option`を`Result`へ変換する
  - 戻り値の型から、呼び出し側が処理すべきケースを読み取る
- [ ] `?`演算子でエラーを伝播する
  - ネストした`match`を`?`で書き換える
  - `?`がエラー時に早期returnし、成功時に中身を取り出すことを確認する
  - `From`によるエラー型の自動変換を確認する
  - `main`から`Result`を返す小さなCLIを書く
- [ ] 独自エラー型を設計する
  - 発生し得る失敗をvariantにした`enum`を定義する
  - `Display`と`std::error::Error`を実装するか、`thiserror`で導出する
  - 元エラーをsourceとして保持し、原因の連鎖を失わないようにする
  - 呼び出し側が回復可能なエラーと、表示するだけのエラーを区別する
- [ ] panicと回復可能なエラーを使い分ける
  - `panic!`、`unwrap`、`expect`で異常終了する例を確認する
  - 外部入力、I/O、ネットワークの失敗には原則`Result`を使う
  - 到達不能な状態やプログラムの不変条件違反にpanicを限定する
  - テストで`#[should_panic]`を使い、panicが仕様となる場合を確認する

### 演習

- [ ] テキストファイルの単語頻度を集計する
- [ ] JSONファイルを読み込む
- [ ] 設定値を検証する
- [ ] 複数種類の失敗を独自エラー型で表現する

Goで頻出するエラー処理は、Rustでは次のように書けます。

```go
value, err := load()
if err != nil {
    return err
}
```

```rust
let value = load()?;
```

短く書くことだけでなく、エラー型をAPIの一部として設計することを意識します。

## 第5週：トレイトとジェネリクス

### 学習項目

- [ ] `trait`で共通の振る舞いを定義する
  - 必須メソッドとデフォルト実装を持つtraitを書く
  - 複数の異なる型へ同じtraitを実装する
  - traitを利用する側が具象型の詳細に依存しないことを確認する
  - traitまたは型のどちらかが自分のcrateに必要というorphan ruleを理解する
- [ ] ジェネリクスで型に依存しない処理を書く
  - ジェネリック関数、構造体、`impl<T>`を一つずつ作る
  - 異なる型で同じ実装を再利用する
  - 何でも受け入れるのではなく、処理に必要な能力をtrait boundで制約する
- [ ] trait boundの書き方を使い分ける
  - `fn f<T: Trait>`と`fn f(value: impl Trait)`を試す
  - 複数の制約を`+`で指定する
  - 複雑な制約を`where`句へ移して読みやすさを比較する
  - conditional implementationで、条件を満たす型にだけメソッドを提供する
- [ ] 標準的なtraitを`derive`する
  - `Debug`、`Clone`、`PartialEq`、`Eq`、`Default`を導出する
  - テストの比較やログ出力で必要になるtraitを確認する
  - `Copy`を付けてよい、小さく単純な値型の条件を理解する
  - 意味が不適切なtraitまで機械的に導出しない
- [ ] 変換traitを使って境界を明確にする
  - 常に成功する変換を`From`として実装する
  - 検証が必要な変換を`TryFrom`として実装する
  - 呼び出し側で`into()`と`try_into()`を使う
  - ライブラリAPIの引数で`impl Into<T>`を使う利点と、過剰に使う欠点を確認する
- [ ] `Display`と`Debug`を使い分ける
  - 開発者向けの出力に`#[derive(Debug)]`と`{:?}`を使う
  - 利用者向けの表示に`Display`と`{}`を実装する
  - `format!`、`println!`、エラーメッセージから両者を利用する
- [ ] 静的ディスパッチと動的ディスパッチを選択する
  - ジェネリクスまたは`impl Trait`で静的ディスパッチを書く
  - `Box<dyn Trait>`と`&dyn Trait`で異なる具象型を同じコレクションに格納する
  - 静的ディスパッチは最適化しやすく、trait objectは実行時の柔軟性があると説明する
  - object safetyに違反するメソッドを試し、trait objectの制約を確認する

### 演習

- [ ] 複数形式に出力できるレポート生成器を作る
- [ ] インメモリ保存とファイル保存を切り替えられるRepositoryを作る
- [ ] `TryFrom`を使って入力値を検証する
- [ ] テスト用のRepository実装を作る

Goのinterfaceに近いものがtraitですが、Rustではジェネリクス、関連型、変換traitも含めて設計します。

## 第6週：スマートポインタと並行処理

### 学習項目

- [ ] `Box<T>`でヒープ確保と再帰型を扱う
  - 値を`Box::new`でヒープへ置き、参照外しで利用する
  - サイズがコンパイル時に決まらない再帰型を`Box`で定義する
  - `Box<dyn Trait>`が所有権を持つtrait objectになることを確認する
  - 大きな値だからという理由だけで安易に`Box`を使わない
- [ ] `Rc<T>`と`Arc<T>`で共有所有権を表現する
  - `Rc::clone`と`Rc::strong_count`で参照カウントの変化を確認する
  - `Rc<T>`が単一スレッド専用であることをコンパイルエラーから確認する
  - 複数スレッドで共有する場合は`Arc<T>`を使う
  - 参照cycleによるリークと`Weak<T>`の役割を理解する
- [ ] interior mutabilityを理解する
  - `RefCell<T>`で実行時に借用規則が検査される例を作る
  - 規則違反がコンパイルエラーではなくpanicになることを確認する
  - `Mutex<T>`で共有データへの排他的アクセスを実装する
  - 単純な数値の共有にはatomic型も候補になることを知る
- [ ] OSスレッドを生成して終了を待つ
  - `std::thread::spawn`で処理を実行し、`JoinHandle::join`で待機する
  - `move`クロージャで必要な値の所有権をスレッドへ移す
  - 複数スレッドで処理を分割し、結果を呼び出し元へ集約する
  - スレッド内のpanicを`join`の戻り値として扱う
- [ ] チャネルでメッセージを送受信する
  - `std::sync::mpsc::channel`でsenderとreceiverを作る
  - senderをcloneして複数producerから送信する
  - 全senderをdropするとreceiverの反復が終了することを確認する
  - 共有メモリとメッセージパッシングのどちらが設計に適するか考える
- [ ] `Send`と`Sync`をコンパイラエラーから理解する
  - `Send`は値の所有権を別スレッドへ移せることを表すと説明する
  - `Sync`は`&T`を複数スレッドで共有できることを表すと説明する
  - `Rc<T>`と`Arc<T>`をスレッドへ渡した結果を比較する
  - 通常は`Send`と`Sync`を手動実装せず、構成要素からの自動導出に任せる
- [ ] ロックを安全に扱う
  - `Arc<Mutex<T>>`で共有状態を更新する
  - `MutexGuard`のスコープを限定し、時間のかかる処理の前にロックを解放する
  - 二つのロックを異なる順序で取るデッドロック例を理解する
  - lock poisoningの扱いを確認し、`unwrap`以外の処理方針も考える

### 演習

- [ ] ワーカープールを作る
- [ ] 複数ファイルを並列処理する
- [ ] 複数スレッドで共有するカウンタを作る
- [ ] チャネルで処理結果を集約する

GoのgoroutineよりOSスレッドの生成は重いため、第8週で学ぶ非同期処理と使い分けます。

## 第7週：タスク管理CLI

ここまでに学んだ内容を使い、一つの実用的なアプリケーションを完成させます。

### 学習項目

- [ ] CLIの責務を層へ分割する
  - 引数解析、ビジネスロジック、永続化、表示を別moduleに分ける
  - `main.rs`は依存関係の組み立てと終了コードの決定に留める
  - ビジネスロジックを`lib.rs`側に置き、コマンドを起動せずテストできるようにする
- [ ] `clap`でコマンドライン仕様を型として表現する
  - derive APIでsubcommandとoptionを定義する
  - 必須引数、任意引数、default値、helpを設定する
  - 不正な入力時のメッセージと終了コードを確認する
- [ ] `serde`で永続化形式とRustの型を変換する
  - タスク型に`Serialize`と`Deserialize`を導出する
  - JSONへのserializeとJSONからのdeserializeを行う
  - ファイルがない初回起動と、壊れたJSONを別のケースとして処理する
  - 書き込み途中の破損を避けるため、一時ファイルから置換する方式を検討する
- [ ] アプリケーションのエラー境界を設計する
  - domain層と永続化層では具体的なエラー型を返す
  - `main`付近でエラーへ文脈を付け、人が読めるメッセージにする
  - 利用者の入力ミスと内部エラーで終了コードを分ける
- [ ] CLIを異なる粒度でテストする
  - 純粋なdomain処理を単体テストする
  - 一時ディレクトリを使って保存と読み込みをテストする
  - binaryを起動する結合テストで標準出力、標準エラー、終了コードを検証する

### 実装する機能

- [ ] タスクを追加する
- [ ] タスクを一覧表示する
- [ ] タスクを完了状態にする
- [ ] タスクを削除する
- [ ] JSONファイルに保存する
- [ ] コマンドライン引数を解析する
- [ ] 入力値を検証する
- [ ] 単体テストと結合テストを書く

### 使用候補のcrate

- `clap`
- `serde`
- `serde_json`
- `thiserror`
- `anyhow`

ライブラリ内部のエラーには`thiserror`、アプリケーション最上位のエラー処理には`anyhow`を使うのが一つの目安です。

## 第8週：非同期処理とWeb API

第7週のタスク管理アプリケーションをWeb APIへ発展させます。

### 学習項目

- [ ] `Future`と非同期処理の基本を理解する
  - `Future`は将来利用可能になる値を表すことを理解する
  - Futureはpollされるまで進まず、完了または待機を`Poll`で表すことを知る
  - CPU処理を速くする仕組みではなく、主にI/O待ちを効率化する仕組みだと説明する
  - Futureを手動実装する必要はないが、executorがpollする構造を図示できるようにする
- [ ] `async`と`.await`で非同期関数を組み立てる
  - `async fn`を定義し、返り値がFutureになることを確認する
  - `.await`で処理を待ち、待機中にexecutorへ制御が戻ることを理解する
  - 独立した処理を`tokio::join!`で並行に進める
  - `tokio::spawn`したtaskの`JoinHandle`を回収し、エラーを無視しない
- [ ] Tokioランタイムを利用する
  - `#[tokio::main]`でruntimeを開始する
  - 非同期timer、channel、I/Oを一つずつ試す
  - blocking処理をasync task内で直接実行せず、`spawn_blocking`を使う
  - taskのキャンセルとtimeoutを試す
- [ ] スレッド、非同期task、並列処理を使い分ける
  - I/O boundな大量の待機処理には非同期taskを選ぶ
  - CPU boundな処理は専用スレッドまたはRayonなどへの分離を検討する
  - `std::sync::Mutex`を`.await`をまたいで保持する問題を理解する
  - goroutineとの類似点に加え、runtimeや`Send`制約の違いを説明する
- [ ] AxumでHTTP APIを構成する
  - `Router`へrouteとhandlerを登録する
  - `Path`、`Query`、`Json` extractorで入力を型として受け取る
  - responseへ適切なstatus codeとJSON bodyを設定する
  - domain errorをHTTP responseへ変換する共通処理を書く
  - middlewareでrequest IDやtracing logを追加する
- [ ] 共有状態と永続化を安全に扱う
  - `State`と`Arc`でrepositoryをhandlerへ渡す
  - インメモリ実装ではlockを`.await`の前後まで保持しない
  - SQLite接続poolを作り、queryを非同期に実行する
  - domain層がAxumやDBの具体的な型へ過度に依存しないようにする
- [ ] graceful shutdownを実装する
  - Ctrl+Cなどの終了signalを非同期に待つ
  - 新規requestの受付を止め、処理中requestの完了を待つ
  - background taskやDB connectionが安全に終了することを確認する
  - shutdown処理にtimeoutを設けるべき理由を説明する

### 実装する機能

- [ ] タスクのCRUD API
- [ ] JSONのリクエストとレスポンス
- [ ] 適切なHTTPステータスとエラーレスポンス
- [ ] インメモリまたはSQLiteへの永続化
- [ ] APIの結合テスト

### 使用候補のcrate

- `tokio`
- `axum`
- `serde`
- `sqlx`または`rusqlite`
- `tracing`

## 推奨リポジトリ構成

各週の演習を独立したCargo packageにし、ルートをCargo workspaceとして管理します。

```text
study_rust/
├── README.md
├── CURRICULUM.md
├── Cargo.toml
├── exercises/
│   ├── 01_basics/
│   ├── 02_ownership/
│   ├── 03_types/
│   ├── 04_errors/
│   ├── 05_traits/
│   └── 06_concurrency/
├── projects/
│   ├── task-cli/
│   └── task-api/
└── notes/
    ├── go-to-rust.md
    └── compiler-errors.md
```

## 学習時のルール

- コンパイラエラーを検索する前に、値の所有者と借用範囲を確認する
- 最初から`clone()`で問題を回避しない
- 引数を所有する必要がなければ、`String`より`&str`を検討する
- インデックス操作よりイテレータを優先する
- `unwrap()`は初期演習に限定し、徐々に`Result`へ置き換える
- 演習ごとに`cargo fmt`、`cargo clippy`、`cargo test`を実行する
- unsafe Rustはこのカリキュラム完了後に学ぶ

## 各週の完了条件

各週は次の条件をすべて満たしたら完了とします。

- [ ] 演習がすべて動作する
- [ ] `cargo fmt --check`が成功する
- [ ] `cargo clippy -- -D warnings`が成功する
- [ ] `cargo test`が成功する
- [ ] Goとの違いを一つ以上`notes/go-to-rust.md`に記録する
- [ ] 遭遇した重要なコンパイルエラーを`notes/compiler-errors.md`に記録する
